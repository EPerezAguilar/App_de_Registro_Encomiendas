import mysql.connector
database = mysql.connector.connect(
    host = "localhost", 
    user = "root",
    password = "",
    database = "alojamiento información encomiendas 1"
)
#Acá se enlaza la base de datos (MySQL)

